library my_globals.globals;

import 'package:flutter/material.dart';

Color buttonBackColor = Colors.deepPurpleAccent;
Color buttonForegColor = Colors.white;
Color submitButtonBackColor = Colors.blueAccent;

// import 'package:flutter_barcode_scanner/flutter_barcode_scanner.dart';
// import 'package:synny_space/model/storage_card.dart';
// int number = 0;
// late StorageCard listItemToEdit;
// _findCard(int? barcode, List<StorageCard> listOfItems) async {
//   for (StorageCard item in listOfItems) {
//     if (item.barcode == barcode) {
//       return listItemToEdit = item;

//       //print('INDEX^^^:::::::::::::::::::::$itemIndex');
//     }
//   }
// }

// _scanBarcode() async {
//   String scannedBarcode = await FlutterBarcodeScanner.scanBarcode(
//       '#ff6666', 'Cancel', true, ScanMode.BARCODE);
//   return (int.parse(scannedBarcode));
// }
